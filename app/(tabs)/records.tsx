import { ScreenContainer } from '@/components/ScreenContainer';

export default function RecordsScreen() {
  return (
    <ScreenContainer
      title="Records"
      subtitle="Scanned receipts and verified service history"
    />
  );
}
